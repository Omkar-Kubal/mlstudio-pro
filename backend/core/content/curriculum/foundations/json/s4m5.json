{
    "meta": {
        "subject": "machine-learning",
        "module": "dimensionality-reduction",
        "level": "intermediate",
        "estimatedHours": 4
    },
    "overview": [
        "Dimensionality reduction is the process of reducing the number of input variables in a dataset while preserving as much meaningful information as possible.",
        "In this module, we explore how to combat the 'Curse of Dimensionality' using projection techniques like PCA and manifold learning techniques like t-SNE and UMAP."
    ],
    "topics": [
        {
            "title": "Principal Component Analysis (PCA)",
            "theory": [
                "PCA is a linear technique that rotates the data into a new coordinate system where the first axis (First Principal Component) captures the maximum possible variance. Subsequent components capture the remaining variance in orthogonal directions.",
                "By dropping the components with low variance, we can reduce the dimensionality of the data while losing very little information. It's essentially 'squashing' the data into fewer dimensions."
            ],
            "visualIntuition": [
                "Imagine a cloud of points shaped like a football. PCA identifies the long axis of the football and the cross-section. Instead of recording 3D coordinates, you just record the position along those 2-3 main axes."
            ],
            "visualSuggestions": [
                "3D point cloud being projected onto a 2D plane with lines showing the principal axes.",
                "Scree plot showing 'Explained Variance' vs Number of Components."
            ],
            "code": [
                {
                    "language": "python",
                    "description": "Applying PCA with Scikit-Learn",
                    "content": "from sklearn.decomposition import PCA\nimport numpy as np\n\nX = np.random.rand(10, 5) # 5 features\npca = PCA(n_components=2)\nX_reduced = pca.fit_transform(X)\nprint(f\"Original shape: {X.shape}\")\nprint(f\"Reduced shape: {X_reduced.shape}\")\nprint(f\"Explained variance: {sum(pca.explained_variance_ratio_):.2f}\")"
                }
            ]
        },
        {
            "title": "t-SNE and Manifold Learning",
            "theory": [
                "Unlike PCA, t-SNE is a non-linear technique designed specifically for visualization. it attempts to preserve local relationships—points that are close together in high-dimensional space remain close in 2D or 3D.",
                "t-SNE works by converting distances into probabilities and then minimizing the difference between those probabilities in the lower-dimensional space. It is excellent for identifying clusters in complex data."
            ],
            "visualIntuition": [
                "Imagine trying to flatten a world globe into a map. PCA is like taking a photo from above (linear projection), while t-SNE is like peeling the globe and stretching it carefully so that neighboring countries stay neighbors."
            ],
            "visualSuggestions": [
                "Animated 'evolution' of t-SNE as clusters form over iterations.",
                "Comparison of PCA vs t-SNE on the MNIST digits dataset."
            ],
            "code": [
                {
                    "language": "python",
                    "description": "t-SNE visualization (Simulation)",
                    "content": "from sklearn.manifold import TSNE\nimport numpy as np\n\nX = np.random.rand(50, 10)\ntsne = TSNE(n_components=2, perplexity=5)\nX_vis = tsne.fit_transform(X)\nprint(\"t-SNE complete. Points mapped to 2D for visualization.\")"
                }
            ]
        },
        {
            "title": "UMAP (Uniform Manifold Approximation)",
            "theory": [
                "UMAP is a modern alternative to t-SNE that is faster and often preserves more of the 'global' structure of the data. It uses concepts from algebraic topology to model the manifold structure.",
                "UMAP is increasingly preferred for large-scale single-cell biology and NLP datasets because it scales better to millions of points than t-SNE."
            ],
            "visualIntuition": [
                "Think of a social network. t-SNE is great at showing your immediate friend group (local), but UMAP is better at showing how your entire neighborhood relates to the whole city (global structure)."
            ],
            "visualSuggestions": [
                "Side-by-side comparison of t-SNE vs UMAP on a complex 'Swiss Roll' dataset.",
                "Scalability benchmark graph showing runtime vs number of points."
            ],
            "code": [
                {
                    "language": "python",
                    "description": "UMAP usage (Concept)",
                    "content": "# Note: Requires 'pip install umap-learn'\n# import umap\n# reducer = umap.UMAP()\n# embedding = reducer.fit_transform(X)\nprint(\"UMAP is used similarly to Scikit-Learn's fit_transform API.\")"
                }
            ]
        }
    ],
    "quiz": [
        "What is the difference between a 'linear' and 'non-linear' dimensionality reduction technique?",
        "Explain what a Principal Component represents in PCA.",
        "Why is t-SNE considered a visualization tool rather than a general dimensionality reduction tool?",
        "How do you determine the optimal number of components in PCA using a Scree Plot?"
    ],
    "references": [
        {
            "label": "Original t-SNE Paper (van der Maaten & Hinton)",
            "url": "https://www.jmlr.org/papers/volume9/vandermaaten08a/vandermaaten08a.pdf"
        },
        {
            "label": "StatQuest: Principal Component Analysis (PCA)",
            "url": "https://www.youtube.com/watch?v=FgakZw6K1QQ"
        },
        {
            "label": "UMAP Documentation - How UMAP Works",
            "url": "https://umap-learn.readthedocs.io/en/latest/how_umap_works.html"
        }
    ],
    "_contentWarnings": []
}